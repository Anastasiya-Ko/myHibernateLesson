# Конфигурация источника данных (Data Source)
hibernate.driver_class=org.postgresql.Driver
hibernate.connection.url=jdbc:postgresql://localhost:5432/myHibernateLesson
hibernate.connection.username=postgres
hibernate.connection.password=admin

#Конфигурация самого Hibernate

#Диалект(к какому типу реалиционной бд мы подключаемся)
hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

#Показывает в консоле генерируемый hibernate sql-код(по умолчанию false)
hibernate.show_sql=true

#
hibernate.current_session_context_class=thread
